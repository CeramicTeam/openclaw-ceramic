{
  "id": "ceramic-search",
  "name": "Ceramic Search",
  "description": "Web search for AI agents via Ceramic's MCP server. Registers Ceramic as a tools.web.search.provider option.",
  "configSchema": {
    "type": "object",
    "properties": {
      "apiKey": { "type": "string", "description": "Ceramic API key (falls back to CERAMIC_API_KEY env var)" }
    },
    "additionalProperties": false
  },
  "contracts": {
    "webSearchProviders": ["ceramic"]
  }
}
